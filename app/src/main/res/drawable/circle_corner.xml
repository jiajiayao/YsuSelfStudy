<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 布局颜色 -->
    <solid android:color="#ffffff"
        />

    <!-- 直接写成android:radius="5dp" 也可以 -->
    <corners
       android:radius="30dp"
        />
</shape>
